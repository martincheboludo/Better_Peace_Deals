BPD_Peace_Deal_Manager = {

	show_hide_peace_decisions = {
        priority = 100
        visible = { always = yes }
		
		ai_will_do = {
			factor = 0
		}

        complete_effect = { 
            if = {
                limit = { has_country_flag = show_peace_decisions }
                clr_country_flag = show_peace_decisions
            }
            else = {
                set_country_flag = show_peace_decisions
            }
        }
    }
	
	show_hide_claim_decisions2 = {
        priority = 100
        visible = { always = yes }
		
		ai_will_do = {
			factor = 0
		}

        complete_effect = { 
            if = {
                limit = { has_country_flag = show_claim_decisions }
                clr_country_flag = show_claim_decisions
            }
            else = {
                set_country_flag = show_claim_decisions
            }
        }
    }

	ITA_Italian_Demands_ETH = {
		icon = GFX_decision_eng_trade_unions_demand
		
		allowed = {
			tag = ITA
		}

		available = {
			has_war_with = ETH
			any_state = { OR = { is_core_of = ETH is_core_of = SOM } NOT = { is_core_of = ITA } NOT = { is_claimed_by = ITA } }
		}

		visible = {
		NOT = { has_country_flag = show_claim_decisions }
		}

		cost = 50
		days_remove = 30

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}
		
		complete_effect = {
		}

		remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			every_state = {
				limit = { OR = { is_core_of = ETH is_core_of = SOM } NOT = { is_core_of = ITA } }
				add_claim_by = ITA
			}
		}
		
	}
	
	ITA_Italian_Demands_TUN = {
		icon = GFX_decision_eng_trade_unions_demand
		
		allowed = {
			tag = ITA
		}

		available = {
			has_war_with = FRA
			any_state = { is_core_of = TUN NOT = { is_core_of = ITA } NOT = { is_claimed_by = ITA } }
		}

		visible = {
		NOT = { has_country_flag = show_claim_decisions }
		}

		cost = 50
		days_remove = 30

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}
		
		complete_effect = {
		}

		remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			every_state = {
				limit = { is_core_of = TUN NOT = { is_core_of = ITA } }
				add_claim_by = ITA
			}
		}
		
	}

	ITA_Italian_Demands_EGY = {
		icon = GFX_decision_eng_trade_unions_demand
		
		allowed = {
			tag = ITA
		}

		available = {
			has_war_with = ENG
			any_state = { OR = { is_core_of = EGY is_core_of = SUD } NOT = { is_core_of = ITA } NOT = { is_claimed_by = ITA } }
		}

		visible = {
		NOT = { has_country_flag = show_claim_decisions }
		}

		cost = 50
		days_remove = 30

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				AND = {
					country_exists = SOV
					NOT = { SOV = { is_in_faction_with = ITA } }
				}
				factor = 0
			}
		}
		
		complete_effect = {
		}

		remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			every_state = {
				limit = { OR = { is_core_of = EGY is_core_of = SUD } NOT = { is_core_of = ITA } }
				add_claim_by = ITA
			}
		}
		
	}

	BPD_restore_status_quo_td_attacker = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			NOT = { has_country_flag = show_peace_decisions }
        }
        target_trigger = {
            FROM = {
                has_war_with = ROOT
				OR = {
					is_in_faction = no
					is_faction_leader = yes
				}
            }
        }
        cost = 25
		modifier = { }
		days_re_enable = 180
		
		ai_will_do = {
			factor = 0
		}

        available = {
		
			OR = {
				is_in_faction = no
				is_faction_leader = yes
			}
		
			war_length_with = {
				tag = FROM
				months > 5
			}

        }
		complete_effect = {
			FROM = { country_event = { id = BPD_Better_Peace_Deals_Country.1 hours = 6 } }
		}
    }
	
	BPD_secure_our_claims_td_attacker = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			NOT = { has_country_flag = show_peace_decisions }
        }
        target_trigger = {
            FROM = {
                has_war_with = ROOT
				OR = {
					is_in_faction = no
					is_faction_leader = yes
				}
            }
        }
        cost = 25
		modifier = { }
		days_re_enable = 180
		
		ai_will_do = {
			factor = 0
		}

        available = {
		
			OR = {
				is_in_faction = no
				is_faction_leader = yes
			}
		
			war_length_with = {
				tag = FROM
				months > 5
			}

        }
		complete_effect = {
			FROM = { country_event = { id = BPD_Better_Peace_Deals_Country.2 hours = 6 } }
		}
    }
	
	BPD_peace_current_borders_td_attacker = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			NOT = { has_country_flag = show_peace_decisions }
        }
        target_trigger = {
            FROM = {
                has_war_with = ROOT
				OR = {
					is_in_faction = no
					is_faction_leader = yes
				}
            }
        }
        cost = 25
		modifier = { }
		days_re_enable = 180
		
		ai_will_do = {
			factor = 0
		}

        available = {
		
			OR = {
				is_in_faction = no
				is_faction_leader = yes
			}
		
			war_length_with = {
				tag = FROM
				months > 5
			}

        }
		complete_effect = {
			FROM = { country_event = { id = BPD_Better_Peace_Deals_Country.3 hours = 6 } }
		}
    }
	
	BPD_secure_our_claims_td_loser = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			NOT = { has_country_flag = show_peace_decisions }
        }
        target_trigger = {
            FROM = {
                has_war_with = ROOT
				OR = {
					is_in_faction = no
					is_faction_leader = yes
				}
            }
        }
        cost = 25
		modifier = { }
		days_re_enable = 180
		
		ai_will_do = {
			factor = 0
		}

        available = {
		
			OR = {
				is_in_faction = no
				is_faction_leader = yes
			}
		
			war_length_with = {
				tag = FROM
				months > 5
			}

        }
		complete_effect = {
			FROM = { country_event = { id = BPD_Better_Peace_Deals_Country.5 hours = 6 } }
		}
    }
	

	
}